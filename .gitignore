node_modules
.next
dist
coverage

.env
.env.local
.env.*.local
!.env.example

prisma/migrations
*.log

.vercel

.claude

# Local npm overrides — .npmrc is intentionally tracked for Vercel builds
# (Do NOT add .npmrc here)
